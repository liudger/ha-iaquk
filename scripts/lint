#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

if ! command -v uv >/dev/null 2>&1; then
    echo "uv is required. Run scripts/setup first." >&2
    exit 1
fi

export UV_PROJECT_ENVIRONMENT="${PWD}/venv"
SKIP=pytest uv run --group dev prek run --all-files
